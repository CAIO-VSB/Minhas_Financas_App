
-- Nova forma de importar plugins
const { $authClient } = useNuxtApp()

-- comando para criar uma migration em sql puro
npm run migrate create -- create_table_ banks_accounts --migration-file-linguage sql

import { useInvalidate } from "~/composables/useInvalidate"
const { invalidate } = useInvalidate()

Lista de coisas a ser fazer (para não cair no esquecimento)

-- Tentar componetizar a sideBar - Prioridade baixa

-- Refactorar sql - finalizar
-- corrgir bug de nao atualizar em tempo real cartao